# Backend EMR Finanças

O backend usa Java 17, Spring Boot 3.2.5, Maven, PostgreSQL e Flyway.

Consulte o `README.md` na raiz do projeto para configuração de ambiente, banco, execução local, testes e estratégia de migrations.
